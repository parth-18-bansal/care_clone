# name: Deploy Preview

# on:
#   issue_comment:
#     types: [created]

# jobs:
#   deploy:
#     if: contains(github.event.comment.body, '/preview up')
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v3

#       - name: Install cloudflared
#         run: |
#           wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
#           sudo dpkg -i cloudflared-linux-amd64.deb

#       - name: Install Docker Compose
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y docker-compose-plugin

#       - name: Start Cloudflare Tunnel
#         id: tunnel
#         run: |
#           # Start cloudflared in background and write logs to a file
#           cloudflared tunnel --url http://localhost:9000 --no-autoupdate > cloudflared.log 2>&1 &
#           # Wait a few seconds for it to initialize
#           sleep 5
#           # Extract the tunnel URL from the logs
#           url=$(grep -o "https://.*trycloudflare.com" cloudflared.log | head -n 1)
#           if [ -z "$url" ]; then echo "Tunnel URL not found"; exit 1; fi
#           echo "Tunnel URL: $url"
#           echo "TUNNEL_URL=$url" >> $GITHUB_ENV

#       - name: Post comment with URL
#         if: github.event.issue.pull_request != null
#         run: |
#           gh pr comment ${{ github.event.issue.number }} \
#             --body "🚀 Preview available at: ${{ env.TUNNEL_URL }}"
#         env:
#           GITHUB_TOKEN: ${{ secrets.PR_COMMENT_PAT }}

#       - name: Run the app
#         run: |
#           docker compose -f docker-compose.yaml -f docker-compose.local.yaml up

